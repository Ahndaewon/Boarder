<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="BoarderDao"><!-- 인터페이스 이름 적어줌 -->
  
	<insert id="insertArticle" parameterType="ArticleVO">
	
		INSERT INTO ARTICLE (
			ID
			, MEMBER_ID
			, TITLE
			, WRITE_DATE
			, VIEW_COUNT
			, RECOMMEND_COUNT
			, REQUEST_IP
			, BODY
		)
		
		VALUES (
			ARTICLE_SEQ.NEXTVAL
			, #{memberId}	
			, #{title}	
			, SYSDATE		
			, 0		
			, 0		
			, #{requestIp}		
			, #{body}
		)
	
	
	</insert>
  	
  	
  	<select id="selectAll" resultType="ArticleVO">
  		SELECT	ID					id
				, MEMBER_ID			memberId
				, TITLE				title
				, TO_CHAR(WRITE_DATE, 'YYYY-MM-DD') 	writeDate
				, VIEW_COUNT 		viewCount
				, RECOMMEND_COUNT 	recommendCount
				, BODY 				body
  		FROM	ARTICLE
  	</select>
  	
  	
  	<select id="selectViewPage" parameterType="_int" resultType="ArticleVO">
  	
  		SELECT	*
  		FROM	ARTICLE
  		WHERE	ID = #{id}
  	
  	</select>

  
  </mapper>
  